class PersonalityModel {
  String title;
  PersonalityModel({this.title});
}

class ExperienceModel{
  String title;
  String description;
  String timeSession;
  bool isSelected;


  ExperienceModel({this.title,this.description,this.timeSession,this.isSelected});
}